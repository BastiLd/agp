# Klartext des privaten Bereichs — darf NIE hochgeladen werden.
# Veröffentlicht wird ausschließlich die verschlüsselte data/private.enc
/data/private.json

# Dasselbe Prinzip für die Kartenbot-Zeitleiste — Klartext bleibt lokal,
# veröffentlicht wird nur data/kartenbot-zeitleiste.enc
/data/kartenbot-zeitleiste.json

# Abhängigkeiten und Build-Ausgaben
node_modules/
.next/
.venv/
venv/
__pycache__/
*.pyc

# Betriebssystem
desktop.ini
Thumbs.db
.DS_Store

# Zugangsdaten (Beispieldateien bleiben erhalten)
.env
!.env.example

# Laufzeitdaten
*.log
*.db
*.sqlite
*.sqlite3
